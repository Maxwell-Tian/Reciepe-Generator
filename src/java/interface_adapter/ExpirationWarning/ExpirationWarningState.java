package interface_adapter.ExpirationWarning;

/**
 * The ExpirationWarningState class represents the state of the expiration warning view.
 * It includes data such as error messages for handling UI interactions.
 */
public class ExpirationWarningState {
    private String errorMessage;

    /**
     * Retrieves the error message associated with the current state.
     *
     * @return the error message
     */
    public String getErrorMessage() {return errorMessage;}
}
